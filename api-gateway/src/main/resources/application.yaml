spring:
  application:
    name: api-gateway

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  sql:
    init:
      mode: never

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
  servlet:
    session:
      cookie:
        # Required for frontend authentication across different origins.
        same-site: none
        secure: true

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}

kind: Application

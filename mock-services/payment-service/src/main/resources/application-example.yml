spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:postgresql://localhost:5432/rflow_payments
    username: postgres
    password: DATABASE_PASSWORD

  sql:
    init:
      mode: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8082
